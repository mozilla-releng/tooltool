---
trust-domain: releng

task-priority: lowest
taskgraph:
    repositories:
        releng:
            name: Taskuddler

workers:
    aliases:
        images:
            provisioner: releng-{level}
            implementation: docker-worker
            os: linux
            worker-type: linux
        t-linux:
            provisioner: releng-t
            implementation: docker-worker
            os: linux
            worker-type: linux
        b-linux:
            provisioner: releng-{level}
            implementation: docker-worker
            os: linux
            worker-type: linux
